# Fixture checkouts and diffs are byte-exact test data pinned to the
# generator: no newline conversion, ever. (A CRLF-converted diff.patch
# would change the embedded diff per platform.) Generator source and
# JSON metadata stay under normal text handling.
bench/fixtures/*/checkout/** -text
bench/fixtures/*/diff.patch -text
